// keys would go here for sensitive keys, below is the env key for hellosign api. To run locally, must use your own. The heroku version works with mine.

exports.hellosign = {
  apiKey: process.env.HELLO_API
}